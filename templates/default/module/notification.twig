<div class="notification-container">
    {% if notifications %}
        {% for notification in notifications %}
            <div class="notification {{ notification.getLevel }}">
                {{ notification.getMessage }}
            </div>
        {% endfor %}
    {% endif %}
</div>